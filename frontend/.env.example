# SkillHub Frontend Environment Configuration

# API Base URL
# 本地前端开发直连后端时，默认后端监听 8001
# Docker / Linux 部署时，推荐设置为前端对外访问的同源地址，例如 http://your-server-ip 或 https://your-domain
NEXT_PUBLIC_API_BASE_URL=http://localhost:8001

# 注意：
# 1. 以 NEXT_PUBLIC_ 开头的变量会在客户端暴露
# 2. 业务能力开关由后端 `/api/v1/runtime-config` 提供，不再通过前端 env 声明
# 3. 部署场景下，修改 NEXT_PUBLIC_API_BASE_URL 后需要重新构建前端镜像
# 4. 本地开发修改此文件后需要重启 Next.js 开发服务器
