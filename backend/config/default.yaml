backend: mcp
thread_pool_max_workers: 128

metadata:
  skill_dir: ./skills

mcp:
  transport: stdio
  host: "0.0.0.0"
  port: 8001

http:
  host: "0.0.0.0"
  port: 8002

flow:
  load_skill_metadata:
    flow_content: LoadSkillMetadataOp()

  load_skill:
    flow_content: LoadSkillOp()

  read_reference_file:
    flow_content: ReadReferenceFileOp()

  run_shell_command:
    flow_content: RunShellCommandOp()

  skill_list_resource:
    flow_content: SkillListResourceOp()

  skill_detail_resource:
    flow_content: SkillDetailResourceOp()

  execute_skill:
    flow_content: ExecuteSkillOp()

llm:
  default:
    backend: openai_compatible
    model_name: qwen-flash

  qwen3_30b_instruct:
    backend: openai_compatible
    model_name: qwen3-30b-a3b-instruct-2507

  qwen3_30b_thinking:
    backend: openai_compatible
    model_name: qwen3-30b-a3b-thinking-2507
    params:
      temperature: 0.6

  qwen_flash:
    backend: openai_compatible
    model_name: qwen-flash

embedding_model:
  default:
    backend: openai_compatible
    model_name: text-embedding-v4
    params:
      dimensions: 1024
