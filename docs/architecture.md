# Open SkillHub 两种运行模式架构图

## 当前代码耦合度分析

### 模式一：FlowLLM 独立模式（无认证、无用户隔离）

```
┌──────────────┐     ┌──────────────────────────────────────────────┐
│   CLI/终端    │     │              FlowLLM 框架                    │
│   skillhub   │────▶│  ┌─────────────────────────────────────────┐ │
│   -mcp       │     │  │  SkillHubMcpApp (core/app.py)            │ │
│   transport  │     │  │    ├── mcp.transport=stdio/sse         │ │
│   =stdio/sse │     │  │    ├── ConfigParser                    │ │
└──────────────┘     │  │    └── run_service()                    │ │
                    │  └─────────────────────────────────────────┘ │
                    │                     │                          │
                    │                     ▼                          │
                    │  ┌─────────────────────────────────────────┐ │
                    │  │         MCP Service                    │ │
                    │  │   (load_skill, run_shell_command...)   │ │
                    │  └─────────────────────────────────────────┘ │
                    └──────────────────────────────────────────────┘
                                      │
                                      ▼
                    ┌──────────────────────────────────────┐
                    │   端口 8001 (当 transport=sse)       │
                    │   直接暴露，无认证拦截                 │
                    └──────────────────────────────────────┘
```

### 模式二：FastAPI HTTP/SSE 模式（有认证、有用户隔离）

```
┌──────────────┐     ┌──────────────────────────────────────────────┐
│  外部客户端   │     │              FastAPI 应用                     │
│  (HTTP请求)  │────▶│  ┌─────────────────────────────────────────┐ │
└──────────────┘     │  │  api_app.py: create_application()      │ │
                     │  │    ├── /api/v1/* (REST API)              │ │
                     │  │    ├── /mcp  ──▶ McpAppProxy + HTTP     │ │
                     │  │    └── /sse  ──▶ McpAppProxy + SSE      │ │
                     │  └─────────────────────────────────────────┘ │
                     │                     │                          │
                     │                     ▼                          │
                     │  ┌─────────────────────────────────────────┐ │
                     │  │         McpAppProxy                     │ │
                     │  │  ┌────────────────────────────────────┐  │ │
                     │  │  │ _authorize_mcp_request()          │  │ │
                     │  │  │   ├── 提取 Bearer Token            │  │ │
                     │  │  │   ├── 验证 API Token 格式          │  │ │
                     │  │  │   ├── 数据库验证 Token             │  │ │
                     │  │  │   └── 设置用户上下文 (ContextVar)  │  │ │
                     │  │  └────────────────────────────────────┘  │ │
                     │  └─────────────────────────────────────────┘ │
                     │                     │                          │
                     │                     ▼                          │
                     │  ┌─────────────────────────────────────────┐ │
                     │  │         FlowLLM MCP Service             │ │
                     │  │   (基于 user_context 隔离数据访问)       │ │
                     │  └─────────────────────────────────────────┘ │
                     └──────────────────────────────────────────────┘
                                      │
                                      ▼
                    ┌──────────────────────────────────────┐
                    │   端口 8002 (FastAPI)                │
                    │   认证后转发到 MCP 服务               │
                    └──────────────────────────────────────┘
```

## 代码模块依赖关系（重构后）

```
┌─────────────────────────────────────────────────────────────────────┐
│                        代码模块依赖关系（重构后）                      │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│   ┌─────────────┐                         ┌─────────────┐          │
│   │   main.py   │                         │  api_app.py │          │
│   │ (CLI入口)   │                         │ (FastAPI入口)│          │
│   └──────┬──────┘                         └──────┬──────┘          │
│          │                                       │                  │
│          │  FlowLLM 独立模式                     │  FastAPI 模式    │
│          ▼                                       ▼                  │
│   ┌─────────────────────────────────────────────────────────┐      │
│   │                    core/app.py                           │      │
│   │              Open SkillHubMcpApp (公共)                    │      │
│   │              - fastmcp stub 定义                         │      │
│   │              - Application 子类                          │      │
│   └─────────────────────────────────────────────────────────┘      │
│          │                                       │                  │
│          └───────────────────┬───────────────────┘                  │
│                              ▼                                      │
│                    ┌─────────────────────┐                         │
│                    │   config/           │                         │
│                    │   ConfigParser      │                         │
│                    │   default.yaml      │                         │
│                    └─────────────────────┘                         │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

## 总结表格

| 特性 | FlowLLM 独立模式 | FastAPI HTTP/SSE 模式 |
|-----|-----------------|---------------------|
| **启动方式** | `skillhub-mcp` CLI | `uvicorn skillhub.api_app` |
| **认证** | ❌ 无 | ✅ Bearer Token |
| **用户隔离** | ❌ 无 | ✅ ContextVar |
| **端口** | 8001 (当 SSE) | 8002 |
| **代码耦合** | 解耦 | 共享 core/app.py |

## 重构前后对比

| 特性 | 重构前 | 重构后 |
|-----|-------|-------|
| Open SkillHubMcpApp 位置 | main.py | core/app.py |
| FastAPI 模式导入 | from main import | from core.app import |
| main.py 职责 | 类定义 + CLI | 纯 CLI |
| 耦合度 | 中等 | 低 |

## 结论

✅ 当前架构符合设计要求：
- **FlowLLM 独立模式**：无认证、无用户隔离，适合本地/开发场景
- **FastAPI HTTP/SSE 模式**：有认证、有用户隔离，适合生产环境
- **Open SkillHubMcpApp** 已剥离到 `core/app.py`，两种模式共享此公共模块
- 两种模式耦合度低，认证逻辑在 FastAPI 层独立处理
